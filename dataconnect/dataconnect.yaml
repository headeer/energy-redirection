specVersion: "v1"
serviceId: "energy-redirection-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "energy-redirection-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
